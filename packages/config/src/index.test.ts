import {describe,expect,it} from "vitest";import {canUseTestPublishingProvider,integrationModeSchema,loadFeatureFlags,redactSecrets} from "./index.js";describe("production config",()=>{it("disables risky features",()=>{const f=loadFeatureFlags({});expect(f.ENABLE_AUTOPILOT).toBe(false);expect(f.ENABLE_REAL_PINTEREST_PUBLISHING).toBe(false);expect(f.ENABLE_REAL_FACEBOOK_PUBLISHING).toBe(false);expect(f.ENABLE_REAL_METRICS_IMPORT).toBe(false);expect(f.ENABLE_SCHEDULED_PUBLISHING).toBe(true)});it("isolates fake publishing from production",()=>{expect(integrationModeSchema.parse(undefined)).toBe("TEST");expect(canUseTestPublishingProvider({NODE_ENV:"test",INTEGRATION_MODE:"TEST",ENABLE_TEST_PUBLISHING_PROVIDER:"true"})).toBe(true);expect(canUseTestPublishingProvider({NODE_ENV:"production",INTEGRATION_MODE:"PRODUCTION",ENABLE_TEST_PUBLISHING_PROVIDER:"true"})).toBe(false)});it("redacts secrets",()=>expect(redactSecrets({accessToken:"secret",nested:{DATABASE_URL:"postgresql://u:p@db/x"}})).toEqual({accessToken:"[REDACTED]",nested:{DATABASE_URL:"[REDACTED]"}}))});
