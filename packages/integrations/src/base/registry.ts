import type{IntegrationProvider,ProviderName}from"./provider.js";export class IntegrationProviderRegistry{readonly #p=new Map<ProviderName,IntegrationProvider>();register(p:IntegrationProvider){if(this.#p.has(p.name))throw new Error("provider_already_registered");this.#p.set(p.name,p);}get(n:string){const p=this.#p.get(n as ProviderName);if(!p)throw new Error("provider_not_configured");return p;}has(n:ProviderName){return this.#p.has(n);}list(){return[...this.#p.keys()];}}
