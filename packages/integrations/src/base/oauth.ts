import{createHash,randomBytes,timingSafeEqual}from"node:crypto";const b=(v:Buffer)=>v.toString("base64url");export function createOAuthProof(){const state=b(randomBytes(32)),codeVerifier=b(randomBytes(64));return{state,codeVerifier,codeChallenge:b(createHash("sha256").update(codeVerifier).digest())};}export function verifyState(a:string,b:string){const x=Buffer.from(a),y=Buffer.from(b);return x.length===y.length&&timingSafeEqual(x,y);}
