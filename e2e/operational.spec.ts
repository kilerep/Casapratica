import{expect,test}from"@playwright/test";
test("núcleo pronto continua em modo assistido sem Mercado Livre",async({page})=>{const failures:string[]=[];page.on("response",response=>{if(response.request().method()==="OPTIONS"&&response.url().includes("product-discovery"))failures.push(`${response.status()} ${response.url()}`)});await page.goto("/app");await expect(page.getByText("CASAPRÁTICA PRONTO PARA USO ASSISTIDO")).toBeVisible();for(const name of ["Aplicativo e API","Banco de dados","Redis","Worker","Publicação assistida"])await expect(page.getByText(name,{exact:true})).toBeVisible();await expect(page.getByText("Não conectado",{exact:true}).first()).toBeVisible();await page.getByRole("button",{name:"TRABALHE O DIA DE HOJE"}).click();await expect(page.getByText(/Mercado Livre ainda não conectado/)).toBeVisible();await expect(page.getByText(/Adicione um produto manualmente/)).toBeVisible();expect(failures).toEqual([]);await page.goto("/app/publicar");await expect(page.getByRole("button",{name:"Adicionar produto manualmente"})).toBeVisible()});
