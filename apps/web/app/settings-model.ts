export type Overview={mode:"LOCAL"|"TEST"|"SANDBOX"|"PRODUCTION";readiness:{status:"ready"|"degraded"|"not_ready";database:string;redis:string;worker:string;externalPublishing?:string};system:Record<string,string>;integrations:Array<{provider:string;configured:boolean;connected:boolean;status:string;capabilities:Record<string,{available?:boolean;status?:string}>;scopes:string[];lastError:string|null;action:string|null}>;publicSite:{url:string;privacy:string;terms:string}|null;contactEmail:string|null;flags:Record<string,boolean>;alerts:Array<{id?:string;type:string;message:string}>};
export type ChecklistItem={label:string;done:boolean;optional:boolean};
export function operationalReadinessChecklist(data:Overview):ChecklistItem[]{const integration=(provider:string)=>data.integrations.find(item=>item.provider===provider);return[{label:"Banco pronto",done:data.readiness.database==="available",optional:false},{label:"Redis pronto",done:data.readiness.redis==="available",optional:false},{label:"Worker pronto",done:data.readiness.worker==="available",optional:false},{label:"Site público configurado",done:Boolean(data.publicSite),optional:true},{label:"Contato configurado",done:Boolean(data.contactEmail),optional:true},{label:"Pinterest configurado",done:Boolean(integration("pinterest")?.configured),optional:true},{label:"Pinterest conectado",done:Boolean(integration("pinterest")?.connected),optional:true},{label:"Facebook configurado",done:Boolean(integration("facebook")?.configured),optional:true},{label:"Mercado Livre configurado",done:Boolean(integration("mercadolivre")?.configured),optional:true},{label:"Publicação externa protegida",done:data.readiness.externalPublishing!=="enabled",optional:false},{label:"Autopilot desligado",done:!data.flags.autopilot,optional:false}]}
export const friendlyState=(value:string|boolean|undefined)=>value===true||value==="available"||value==="connected"||value==="ready"?"OK":value==="degraded"||value==="optional"||value==="pilot_disabled"?"ATENÇÃO":value==="not_configured"||value===undefined?"NÃO CONFIGURADO":"INDISPONÍVEL";
