"use client";
import {useEffect,useState} from "react";const api=process.env.NEXT_PUBLIC_API_URL??"http://localhost:3001";type O={classification:string;score:{score:number|null;confidence:number};kpis:Record<string,{value:number|null;availability:string}>;dataQuality:{coverage:number};insights:{title:string;explanation:string}[];message:string|null};export default function AnalyticsPage(){const [d,setD]=useState<O|null>(null),[e,setE]=useState<string|null>(null);useEffect(()=>{const to=new Date(),from=new Date(to.getTime()-604800000);fetch(`${api}/api/analytics/overview?from=${from.toISOString()}&to=${to.toISOString()}`).then(async r=>{if(!r.ok)throw new Error("Analytics indisponível");setD(await r.json() as O)}).catch(x=>setE(x instanceof Error?x.message:"Falha"))},[]);return <main><p className="eyebrow">PERFORMANCE INTELLIGENCE</p><h1>Analytics</h1>{e&&<p role="alert">{e}</p>}{!d&&!e&&<p>Carregando dados históricos…</p>}{d&&<><section className="cards"><article className="card"><strong>Performance Score</strong><h2>{d.score.score??"—"}</h2><small>Confiança {Math.round(d.score.confidence*100)}% · separado do Product Score</small></article><article className="card"><strong>Classificação</strong><h2>{d.classification.toLowerCase().replaceAll("_"," ")}</h2></article><article className="card"><strong>Cobertura</strong><h2>{Math.round(d.dataQuality.coverage*100)}%</h2></article></section><h2>KPIs seguros</h2><div className="cards">{Object.entries(d.kpis).map(([n,k])=><article className="card" key={n}><strong>{n}</strong><h2>{k.value===null?"—":k.value.toFixed(4)}</h2><small>{k.availability}</small></article>)}</div><h2>Insights</h2>{d.insights.length?d.insights.map(i=><article className="card" key={i.title}><strong>{i.title}</strong><p>{i.explanation}</p></article>):<p>Nenhum insight confiável no período.</p>}</>}</main>}
