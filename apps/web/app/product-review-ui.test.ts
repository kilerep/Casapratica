import{readFileSync}from"node:fs";import{describe,expect,it}from"vitest";const page=readFileSync(new URL("./app/produtos/page.tsx",import.meta.url),"utf8"),css=readFileSync(new URL("./styles.css",import.meta.url),"utf8");describe("product review UI",()=>{it("contains missing and comparison empty states",()=>{expect(page).toContain("Dado não disponível");expect(page).toContain("Sem comparação disponível");expect(page).toContain("Nenhum conteúdo criado ainda");expect(page).toContain("Nenhum criativo criado ainda")});it("has human actions but no publishing action",()=>{expect(page).toContain("MANTER COMO TESTE");expect(page).not.toContain("publish-now");expect(page).not.toContain("pinterest/pilot")});it("has a mobile layout",()=>{expect(css).toContain("@media(max-width:760px)");expect(css).toContain(".comparison-grid{grid-template-columns:1fr}")})});
