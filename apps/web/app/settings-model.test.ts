import{describe,expect,it}from"vitest";import{friendlyState,operationalReadinessChecklist,type Overview}from"./settings-model";
const overview=(extra:Partial<Overview>={}):Overview=>({mode:"TEST",readiness:{status:"degraded",database:"available",redis:"available",worker:"degraded",externalPublishing:"disabled"},system:{},integrations:[{provider:"pinterest",configured:false,connected:false,status:"not_configured",capabilities:{},scopes:[],lastError:null,action:null},{provider:"facebook",configured:false,connected:false,status:"not_configured",capabilities:{},scopes:[],lastError:null,action:null},{provider:"mercadolivre",configured:false,connected:false,status:"not_configured",capabilities:{},scopes:[],lastError:null,action:null}],publicSite:null,contactEmail:null,flags:{autopilot:false},alerts:[],...extra});
describe("operational readiness checklist",()=>{it("keeps optional setup visible without changing core readiness",()=>{const items=operationalReadinessChecklist(overview());expect(items).toHaveLength(11);expect(items.find(item=>item.label==="Site público configurado")).toMatchObject({done:false,optional:true});expect(items.find(item=>item.label==="Autopilot desligado")?.done).toBe(true)});it("reflects a connected integration and configured public values",()=>{const data=overview({publicSite:{url:"https://example.test",privacy:"https://example.test/privacidade",terms:"https://example.test/termos"},contactEmail:"contato@example.test"});data.integrations[0]={...data.integrations[0]!,configured:true,connected:true,status:"connected"};const items=operationalReadinessChecklist(data);expect(items.find(item=>item.label==="Pinterest conectado")?.done).toBe(true);expect(items.filter(item=>item.done).length).toBeGreaterThan(4)});it("maps friendly states",()=>{expect(friendlyState("available")).toBe("OK");expect(friendlyState("degraded")).toBe("ATENÇÃO");expect(friendlyState("not_configured")).toBe("NÃO CONFIGURADO");expect(friendlyState("unavailable")).toBe("INDISPONÍVEL")})});
