import{readFileSync}from"node:fs";import{resolve}from"node:path";import{describe,expect,it}from"vitest";const read=(path:string)=>readFileSync(resolve("../..",path),"utf8"),root=JSON.parse(read("package.json"));describe("local startup without tunnel",()=>{it("keeps ngrok completely outside the normal compose",()=>{const normal=read("infra/docker-compose.yml"),oauth=read("infra/docker-compose.oauth.yml");expect(normal).not.toMatch(/ngrok|NGROK|oauth-tunnel/);expect(normal).toContain("postgres:");expect(normal).toContain("redis:");expect(oauth).toContain('profiles: ["oauth-tunnel"]');expect(oauth).toContain("NGROK_AUTHTOKEN")});it("offers one command and a clear Docker message",()=>{const script=read("scripts/start-casapratica.mjs");expect(root.scripts["start:casapratica"]).toBe("node scripts/start-casapratica.mjs");expect(script).toContain("infra/docker-compose.yml");expect(script).toContain("Abra o Docker Desktop e execute novamente.");expect(script).not.toContain("NGROK")});it("doctor marks tunnel optional",()=>{expect(read("scripts/doctor.mjs")).toContain("NGROK/OAUTH TUNNEL — opcional / não configurado")})});
