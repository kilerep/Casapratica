import{readFileSync}from"node:fs";import{resolve}from"node:path";import{describe,expect,it}from"vitest";
const root=JSON.parse(readFileSync(resolve("../../package.json"),"utf8")),turbo=JSON.parse(readFileSync(resolve("../../turbo.json"),"utf8")),api=JSON.parse(readFileSync(resolve("package.json"),"utf8")),web=JSON.parse(readFileSync(resolve("../web/package.json"),"utf8")),script=readFileSync(resolve("../../scripts/dev.mjs"),"utf8");
describe("local dev orchestration",()=>{it("uses persistent Turbo without deprecated parallel mode",()=>{expect(root.scripts.dev).toBe("node scripts/dev.mjs");expect(root.scripts["dev:services"]).toBe("turbo run dev");expect(root.scripts["dev:services"]).not.toContain("--parallel");expect(turbo.tasks.dev).toMatchObject({cache:false,persistent:true})});it("binds fixed ports and avoids the API watcher deadlock",()=>{expect(web.scripts.dev).toContain("--port 3000");expect(api.scripts.dev).toBe("tsx src/server.ts")});it("checks every runtime service and cleans its Windows process tree",()=>{for(const value of ["127.0.0.1:3000","127.0.0.1:3001/health","assisted-publication/products","Worker started","taskkill"])expect(script).toContain(value)})});
