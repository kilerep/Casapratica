import {afterEach,describe,expect,it} from "vitest";import {buildApp} from "./app.js";const apps:ReturnType<typeof buildApp>[]=[];afterEach(()=>Promise.all(apps.splice(0).map(a=>a.close())));describe("readiness",()=>{it("keeps liveness independent from dependencies",async()=>{const app=buildApp();apps.push(app);expect((await app.inject({url:"/health"})).statusCode).toBe(200);expect((await app.inject({url:"/ready"})).statusCode).toBe(503)});it("reports optional integrations without blocking",async()=>{const app=buildApp({readiness:async()=>({status:"degraded",database:"available",redis:"available",worker:"degraded",integrations:{pinterest:"optional",meta:"optional",mercadolivre:"optional"}})});apps.push(app);const response=await app.inject({url:"/ready"});expect(response.statusCode).toBe(200);expect(response.json().status).toBe("degraded")})});
